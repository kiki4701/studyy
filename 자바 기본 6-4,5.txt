자바 기본 6-4,5

필드(지역변수), 메서드 에서 접근제어자를 사용할 때

지역변수에 private 를 사용하게 되면 같은 클래스가 아닌곳에서는 바로 접근이 불가능하다.

하지만 지역변수 private 로 설정해놓고 같은 클래스 내에서 메서드를 public 으로 설정해놓은 뒤 메서드에서 접근은 가능하다.

클래스 레벨의 접근 제어자는 public, default 만 사용할 수 있다.

public 클래스는 한 클래스당 한개만 사용이 가능하고 반드시 파일명과 이름이 같아야 한다.