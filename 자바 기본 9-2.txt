자바 기본 9-2

상속은 객체 지향 프로그래밍의 핵심 요소 중 하나로, 기존 클래스의 필드와 메서드를 새로운 클래스에서 재사용하게 해준다. 부모의 속성과 기능을 자식이 물려받는 것이며, extends 대상은 하나만 선택할 수있다.

화살표는 내가 저 클래스를 안다 라는 뜻이라고 생각!

그러므로 자식인 ElectricCar와 GasCar는 부모인 Car를 알지만 반대로 부모는 자식을 모른다. (부모에는 자식에대한 정보가 아예 없으므로 접근 불가)
