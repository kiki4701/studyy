자바 기본 4-1,2

생성자는 객체 생성 직후 객체를 초기화 하기 위한 특별한 메서드라고 생각할 수 있다.

객체를 생성하는 시점에 어떠한 작업을 하고싶다면 생성자를 이용하면 된다!


this 는 자기자신의 인스턴스를 가리킨다.

멤버 변수와 메서드 안의 매개변수의 매개변수의 이름이 같으면 매개변수가 코드 블럭의 더 안쪽(가까이)에 있기 때문에 매개변수가 우선순위를 가진다. 이런 경우 this. 을 이용하여 인스턴스 자신의 참조값을 가리키면 매개변수에 접근할 수 있다.